# Backend API base URL
API_BASE_URL=http://localhost:4000/api
# Interval in minutes between automatic screenshots
SCREENSHOT_INTERVAL_MINUTES=5
# Interval in seconds for activity sampling (active window, idle time)
ACTIVITY_INTERVAL_SECONDS=15
