const Logo = ({ className = 'text-2xl font-semibold text-primary' }) => (
  <div className={className}>
    <span className="font-bold">Web</span>
    <span className="text-primary-dark">Work</span>
  </div>
);

export default Logo;
